# BOTZD

Telegram bot (TeleBot / pyTelegramBotAPI + SQLite).

## Setup
1) Create `.env` from `.env.example` and fill `BOT_TOKEN` (rotate your token if it was shared).
2) Install deps: `pip install -r requirements.txt`
3) Run: `python -m bot`
